gradio==6.2.0
numpy>=2.0
scipy>=1.10
soundfile>=0.12
setuptools>=65
# Python 3.13 removed the stdlib `audioop`. Some Gradio audio utilities (via pydub) still import it.
audioop-lts; python_version >= "3.13"
mido>=1.3